name: home
description: "A new Flutter package project."
version: 0.0.1
homepage: "Home"
publish_to: none

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  arch:
    path: ../../libs/arch
  res:
    path: ../../libs/res

  flutter_lints: ^3.0.0
  get_it: ^7.6.7
  injectable: any
  http: ^1.2.1
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  injectable_generator: any

flutter:
