name: onboarding
description: Onboarding feature
publish_to: 'none'

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  common:
    path: ../../core/common
  flutter:
    sdk: flutter
  flutter_bloc:
  freezed_annotation:
  ui:
    path: ../../core/ui

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  freezed:
  very_good_analysis:
