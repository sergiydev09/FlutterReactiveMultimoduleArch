/// Icon identifiers for shell menu items.
///
/// Decouples domain entities from Flutter's `IconData`.
/// Mapped to actual icons in the presentation layer.
enum MenuIcon {
  home,
  payments,
  notifications,
  accounts,
  cards,
  settings,
  investments,
  insurance,
  loans,
  offers,
}
