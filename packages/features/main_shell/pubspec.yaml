name: main_shell
description: Main shell feature - app shell with dynamic navigation
publish_to: 'none'

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  common:
    path: ../../core/common
  easy_localization:
  flutter:
    sdk: flutter
  flutter_bloc:
  flutter_riverpod:
  fpdart:
  freezed_annotation:
  go_router:
  security:
    path: ../../core/security
  ui:
    path: ../../core/ui

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  freezed:
  very_good_analysis:
