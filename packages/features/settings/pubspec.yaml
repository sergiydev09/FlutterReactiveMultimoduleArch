name: settings_feature
description: Settings feature
publish_to: 'none'

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  common:
    path: ../../core/common
  flutter:
    sdk: flutter
  flutter_bloc:
  freezed_annotation:
  security:
    path: ../../core/security
  ui:
    path: ../../core/ui

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  freezed:
  very_good_analysis:
