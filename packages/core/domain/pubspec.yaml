name: domain
description: Shared domain entities, value objects, repository contracts
publish_to: 'none'

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  common:
    path: ../common
  equatable:
  fpdart:
  freezed_annotation:

dev_dependencies:
  build_runner:
  freezed:
  very_good_analysis:
